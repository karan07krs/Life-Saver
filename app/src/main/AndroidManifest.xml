<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clinic">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="Life Saver"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userloadingactivity"></activity>
        <activity android:name=".requestFragmentFinalAppointment" />
        <activity android:name=".email_verification" />
        <activity android:name=".profileactivity" />
        <activity android:name=".signupactivity" />
        <activity android:name=".loginactivity" />
        <activity android:name=".DoctorLoginActivity" />
        <activity android:name=".DoctorsProfileActivity" />
        <activity android:name=".AddTodayActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>